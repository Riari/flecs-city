{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch flecs-city as server (Windows)",
            "program": "${workspaceFolder}/build/bin/flecs-city.exe",
            "args": ["--listen"]
        }
    ]
}
